{
  "private": true,
  "scripts": {
    "start": "run-pty % node --run frontend % node --run backend",
    "start-advanced": "run-pty run-pty.json",
    "frontend": "vite --no-clearScreen",
    "backend": "node --watch server.js"
  },
  "devDependencies": {
    "run-pty": "file:..",
    "vite": "7.3.0"
  }
}
